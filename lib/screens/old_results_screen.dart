import 'package:flutter/material.dart';

class OldResultsScreen extends StatelessWidget {
  const OldResultsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Old Results Screen'),
      ),
    );
  }
}
